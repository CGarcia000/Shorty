/*
    TODO - middleware que checa caso é um user logado ou anonimo
    fazendo o request para rotas nao autenticadas

    return {
        isLogged: boolean
        userToken: [int/null] // ou id dependendo da implementacao do jwt
    }
 */